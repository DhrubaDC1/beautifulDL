{
  "name": "local-stream",
  "version": "1.0.0",
  "private": true,
  "description": "YouTube video downloader with FastAPI backend and React frontend",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && source venv/bin/activate && uvicorn main:app --reload --port 8000",
    "frontend": "cd frontend && npm run dev",
    "install:all": "cd frontend && npm install",
    "setup": "cd backend && python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
